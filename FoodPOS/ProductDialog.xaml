<Window x:Class="FoodPOS.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Details - FoodPOS" 
        Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Form Fields -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Product Name:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtName" Height="25" Margin="0,0,0,10"/>

            <TextBlock Text="Description:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtDescription" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Price (Rs.):" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtPrice" Height="25"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Cost Price (Rs.):" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtCostPrice" Height="25"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Category:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbCategory" DisplayMemberPath="Name" Height="25" Margin="0,0,0,10"/>

            <TextBlock Text="Initial Stock Quantity:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtStockQuantity" Height="25" Margin="0,0,0,10"/>

            <TextBlock Text="Barcode:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtBarcode" Height="25" Margin="0,0,0,15"/>

            <CheckBox x:Name="chkIsActive" Content="Product is Active" IsChecked="True" FontWeight="Bold"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="💾 Save" Width="80" Height="30" 
                    Background="#4CAF50" Foreground="White"
                    Click="btnSave_Click" Margin="0,0,10,0"/>
            <Button Content="❌ Cancel" Width="80" Height="30" 
                    Background="#F44336" Foreground="White"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>